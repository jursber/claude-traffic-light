# 旧版 Windows 辅助脚本（不进入 V3 包 / 不进入 PyInstaller）

这些文件曾用于计划任务、NSSM、隐藏窗口启动等，与 **V3 核心逻辑**（`src/claude_tl`）无关。

- 若你仍需要「注册计划任务」类能力，可在此目录按需修改路径后使用。
- 新环境建议优先用根目录 `daemon_guard*.vbs` + `python daemon_unified.py`（薄启动器）。
